<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <!-- Left side: Search bar -->
        <div class="d-flex align-items-center flex-grow-1">
            <form class="d-flex w-100" style="max-width: 300px;">
                <div class="input-group">
                    <span class="input-group-text bg-white border-end-0">
                        <i class="bi bi-search"></i>
                    </span>
                    <input class="form-control border-start-0" type="search" placeholder="search" aria-label="Search">
                </div>
            </form>
        </div>

        <!-- Right side: Login button -->
        <div class="ms-auto">
            {{#if user}}
                <a href="/profile" class="btn btn-link text-dark text-decoration-none">
                    <i class="bi bi-person-circle fs-4"></i>
                    <span>{{ user.username }}</span>
                </a>
            {{else}}
                <a href="/login" class="btn btn-link text-dark text-decoration-none">
                    <i class="bi bi-person-circle fs-4"></i>
                    <span>Login</span>
                </a>
            {{/if}}
        </div>
</nav>